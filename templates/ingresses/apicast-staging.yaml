apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    {{- include "3scale-amp-chart.labels" . | nindent 4 }}
    threescale_component: apicast
    threescale_component_element: staging
  annotations:
    kubernetes.io/ingress.class: "nginx"
  name: apicast-staging
spec:
  tls:
    - hosts:
        - api-{{ .Values.tenantName }}-apicast-staging.{{ .Values.wildcardDomain }}
      secretName: apicast-staging-tls
  rules:
    - host: api-{{ .Values.tenantName }}-apicast-staging.{{ .Values.wildcardDomain }}
      http: 
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: apicast-staging
                port:
                  name: gateway